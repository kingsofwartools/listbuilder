import React, { Fragment } from 'react';
import Index from './Index';

const StandardIndex = () => {
  return (
    <Fragment>
      <Index type="standard" />
    </Fragment>
  );
};

export default StandardIndex;
